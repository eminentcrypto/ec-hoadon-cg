# ECORP Hóa đơn - Netlify frontend

Upload this package to Netlify. The Apps Script `code.gs` is intentionally NOT included in the public publish directory.

Frontend: `public/index.html`
API proxy: `netlify/functions/api.js`
Routing: `netlify.toml`
Headers: `public/_headers`
